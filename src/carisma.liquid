---
layout: default
title: Carisma
---
<div class="container main-content">
  <section class="section">
    <h1 class="custom-hero">Nosso Carisma</h1>

    <p class="custom-text">
      A <strong>Santa Missa</strong> é o centro da nossa vida espiritual: nela nos unimos ao Sacrifício de Cristo e nos
      alimentamos da Sua presença real na <strong>Santíssima Eucaristia</strong>. A adoração ao
      <strong>Santíssimo Sacramento</strong> é fonte diária de graça, onde aprendemos a amar, escutar e servir como
      Maria, em silêncio e adoração.
    </p>

    <br>
    <p class="custom-text">Inspirados nos santos, queremos:</p>

    <ul class="custom-text">
      <li>Viver a pobreza e amor radical de Cristo como <strong>São Francisco de Assis;</strong></li>
      <li>Ser almas de oração e reparação como <strong>São Pio de Pietrelcina;</strong></li>
      <li>
        Consagrar-nos totalmente a Jesus pelas mãos de Maria, como ensinou
        <strong>São Luís Maria Grignion de Montfort;</strong>
      </li>
    </ul>

    <div class="columns is-multiline mt-6">
      {% assign santos = site.data.santos %}
      {% for santo in santos %}
        <div class="column is-one-third has-text-centered">
          <figure class="image is-inline-block is-clickable" onclick="mostrarSanto('{{ forloop.index0 }}')">
            <img class="saint" src="{{ santo.imagem | relative_url }}" alt="{{ santo.nome }}">
          </figure>
          <p class="custom-text is-clickable" onclick="mostrarSanto('{{ forloop.index0 }}')">
            <strong>{{ santo.nome }}</strong>
          </p>
        </div>
      {% endfor %}
    </div>
  </section>
</div>

<div class="modal" id="modal-santo">
  <div class="modal-background" onclick="fecharModal()"></div>
  <div class="modal-card" style="width: 70vw; max-height: 80vh; background: white;">
    <header class="modal-card-head" style="background: white; border: none;">
      <p class="modal-card-title" id="modal-titulo" style="color: #C4B35F;"></p>
      <button class="delete" aria-label="close" onclick="fecharModal()"></button>
    </header>
    <section class="modal-card-body" id="modal-conteudo" style="overflow-y: auto; background: white;"></section>
    <footer
      class="modal-card-foot is-flex is-justify-content-center is-hidden-desktop"
      style="background: white; border: none;"
    >
      <button class="button" style="background-color: #C4B35F; color: black;" onclick="fecharModal()">Fechar</button>
    </footer>
  </div>
</div>

<style>
  .form_title {
    color: black;
    font-size: 40px;
  }

  .image.is-clickable {
    cursor: pointer;
    transition: transform 0.2s ease;
  }
  .image.is-clickable:hover {
    transform: scale(1.05);
  }
  .modal-columns {
    display: flex;
    flex-direction: row;
    gap: 2rem;
    flex-wrap: wrap;
    align-items: flex-start;
  }
  .modal-image {
    flex: 0 0 300px;
    text-align: center;
  }
  .modal-image img {
    width: 100%;
    border-radius: 12px;
  }
  .modal-dados {
    flex: 1;
    font-size: 1.2rem;
    line-height: 1.6;
    text-align: justify;
  }
  @media (max-width: 1024px) {
    .modal-columns {
      flex-direction: column;
      align-items: center;
    }
    .modal-dados {
      text-align: center;
      font-size: 1.1rem;
    }
    .modal-card {
      width: 90vw !important;
      max-height: 90vh !important;
    }
  }
</style>

<script>
  const santos = {{ site.data.santos | jsonify }};

  function mostrarSanto(index) {
    const santo = santos[index];
    document.getElementById("modal-titulo").textContent = santo.nome;
    document.getElementById("modal-conteudo").innerHTML = `
      <div class="modal-columns">
        <div class="modal-image">
          <img src="${santo.imagem}" alt="${santo.nome}">
        </div>
        <div class="modal-dados">
          ${santo.dados ? santo.dados : ''}
        </div>
      </div>
      <hr>
      <p class="mt-4">${santo.resumo}</p>
    `;
    document.getElementById("modal-santo").classList.add("is-active");
  }

  function fecharModal() {
    document.getElementById("modal-santo").classList.remove("is-active");
  }
</script>
