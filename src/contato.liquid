---
layout: default
title: Contato
---
<section class="section">
  <section class="section main-content">
    <div class="container">
      <h1 class="custom-hero">Pedido de Oração ou Dúvidas</h1>
      <div class="columns">
        <div class="column is-half">
          <figure class="image">
            <img src="/images/contato_novo.jpeg" alt="Momento de oração" style="border-radius: 12px;">
          </figure>
        </div>
        <div class="column is-half">
          <form id="contact-form" action="https://formspree.io/f/mqaqprqj" method="POST">
            <div class="field">
              <label class="label">Nome completo:</label>
              <div class="control">
                <input
                  class="input"
                  type="text"
                  name="nome"
                  placeholder="Seu nome completo"
                  required
                  pattern="[A-Za-záéíóúÁÉÍÓÚãõáàâéêíóõô\s]+"
                  title="Por favor, insira um nome válido"
                  minlength="3"
                >
              </div>
            </div>
            <div class="field">
              <label class="label">E-mail:</label>
              <div class="control">
                <input
                  class="input"
                  type="email"
                  name="email"
                  placeholder="Seu e-mail"
                  required
                  pattern="^[^@\s]+@[^@\s]+\.[^@\s]+$"
                  title="Por favor, insira um e-mail válido"
                >
              </div>
            </div>
            <div class="field">
              <label class="label">Cidade e Estado:</label>
              <div class="control">
                <input
                  class="input"
                  type="text"
                  name="cidade"
                  placeholder="Sua cidade e estado"
                  required
                  pattern="[A-Za-záéíóúÁÉÍÓÚãõáàâéêíóõô\s]+"
                  title="Por favor, insira uma cidade válida"
                  minlength="2"
                >
              </div>
            </div>
            <div class="field">
              <label class="label">Paróquia e Comunidade:</label>
              <div class="control">
                <input
                  class="input"
                  type="text"
                  name="paroquia"
                  placeholder="Sua diocese, paróquia e comunidade"
                  required
                  pattern="[A-Za-záéíóúÁÉÍÓÚãõáàâéêíóõô\s]+"
                  title="Por favor, insira o nome da paróquia"
                  minlength="2"
                >
              </div>
            </div>
        </div>
      </div>
      <div class="columns">
        <div class="column is-full">
          <div class="field">
            <label class="label">Pedido de oração ou dúvida:</label>
            <div class="control">
              <textarea
                class="textarea"
                name="mensagem"
                placeholder="Escreva aqui seu pedido ou dúvida..."
                required
                rows="10"
              ></textarea>
            </div>
          </div>
          <div class="field is-grouped">
            <div class="control">
              <button class="button is-warning is-light custom-button" type="submit">Enviar</button>
            </div>
          </div>
          </form>
        </div>
      </div>
    </div>
  </section>
</section>

<script>
  document.getElementById('contact-form').addEventListener('submit', async function (event) {
    event.preventDefault();
    const form = event.target;
    const data = new FormData(form);
    const response = await fetch(form.action, {
      method: form.method,
      body: data,
      headers: { Accept: 'application/json' },
    });
    if (response.ok) {
      form.reset();
      document.getElementById('success-message').style.display = 'block';
    } else {
      alert('Erro no envio. Tente novamente.');
    }
  });

  document.getElementById('close-success-message').addEventListener('click', function () {
    document.getElementById('success-message').style.display = 'none';
  });
</script>

<style>
  .form_title {
    color: black;
    font-size: 40px;
  }

  @media screen and (max-width: 768px) {
    .custom-hero {
      text-align: center;
      font-size: 24px;
    }
  }

  .hero {
    width: 400px !important;
    height: 400px !important;
    object-fit: fill;
    border-radius: 10px;
    margin-bottom: 10px;
    margin-top: 5px;
  }
</style>
